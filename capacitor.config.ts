import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'proyecto_retail_off',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
